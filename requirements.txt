# Core
requests>=2.31
python-dotenv>=1.0

# LLM providers
# Groq is called via its OpenAI-compatible REST API using `requests` (no SDK needed).
# Gemini fallback uses the NEW google-genai SDK (NOT the deprecated google-generativeai).
google-genai>=1.0

# Data / export
pandas>=2.0
openpyxl>=3.1
# PDF findings report (charts via matplotlib, layout via reportlab — both
# pure-Python enough to run on Streamlit Cloud; no headless Chrome needed)
matplotlib>=3.7
reportlab>=4.0

# UI
streamlit>=1.35
plotly>=5.20
